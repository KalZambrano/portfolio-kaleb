---
const {iconSrc, title, level} = Astro.props
---
<article class="w-[136px] h-fit rounded-xl flex flex-col items-center gap-3 border dark:border-gray-700 p-3 cursor-default hover:scale-110 transition-all">
    <h4 class="font-bold">{title}</h4>
    <img class="size-16" src={`/icons/${iconSrc}`} alt={`${title} icon`}>
    <span class="rounded-full dark:text-gray-300 dark:bg-gray-800 px-3 py-1 font-light text-sm">{level}</span>
</article>